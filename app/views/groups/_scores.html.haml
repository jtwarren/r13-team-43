%h2
  Scores

%hr

%table.table.table-striped
  %tbody
    - @group.users.each do |user|
      %tr
        %td.points
          = user.points

        %td
          = render 'layouts/gravatar', email: user.email
