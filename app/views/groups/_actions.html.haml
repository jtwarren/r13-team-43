- if current_user.groups.include?(group)
  = link_to leave_group_path(group), remote: true, method: :put, class: 'btn btn-primary btn-xs leave' do
    %span.glyphicon.glyphicon-minus-sign

    &nbsp;LeaveMe

- else
  = link_to join_group_path(group), remote: true, method: :put, class: 'btn btn-success btn-xs join' do
    %span.glyphicon.glyphicon-plus-sign

    &nbsp;JoinMe
